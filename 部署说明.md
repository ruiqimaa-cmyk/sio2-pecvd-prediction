# SiO2 PECVD 预测网页部署说明

## 已完成

- `index.html`：可直接发布的静态网页
- `public/figures/`：模型结果图
- `supabase/schema.sql`：Supabase 建表脚本
- 已接入 Supabase 项目：`https://rspjfcltiaporaeqwlwt.supabase.co`
- 已能读取/写入 `prediction_logs`

## GitHub Pages 部署

1. 新建 GitHub 仓库，例如 `sio2-pecvd-prediction`
2. 上传本文件夹内所有文件，不要只上传 `index.html`
3. 进入仓库 `Settings -> Pages`
4. Source 选择 `Deploy from a branch`
5. Branch 选择 `main`，目录选择 `/root`
6. 保存后等待 GitHub Pages 生成网页地址

## Vercel 部署

1. 打开 Vercel，新建 Project
2. 选择 `Import Git Repository`，导入上面的 GitHub 仓库
3. Framework 选择 `Other`
4. Build Command 留空
5. Output Directory 留空或填写 `.`
6. Deploy

## 注意

- 当前网页使用 Supabase publishable key，可以公开放在前端。
- 不要把 `sb_secret_...` 写入网页、GitHub 或 Vercel。
- 你之前发过 secret key，建议在 Supabase 里重新生成 secret key。
